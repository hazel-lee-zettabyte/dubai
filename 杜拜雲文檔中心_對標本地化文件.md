# 杜拜雲文檔中心 — 對標本地化交付文件

> **用途**：本文件供 web designer 依此複刻頁面。內容由對標網站（QingCloud 公有雲文檔中心）逐頁整理而來，翻譯為繁體中文；技術／產品專有名詞（如 Redis、Kafka、MongoDB、VPC、EIP、redis-cli、FLUSHALL、ACL 等）一律保留原文。
>
> **品牌替換提醒**：對標網站的品牌為「青雲 QingCloud」。杜拜站請一律替換為「Dubai Hybrid Cloud」；控制台名稱、Logo、頁首文字同步替換。文中凡出現「青雲 QingCloud 管理控制台」，實作時請改為杜拜站對應的控制台名稱。
>
> **狀態**：逐頁累積中。

---

## 頁面清單（隨進度更新）

| # | 頁面 | 板塊路徑 | 狀態 |
|---|------|----------|------|
| 1 | 連接 Redis Cluster 實例 | 鍵值資料庫 Redis Cluster / 快速入門 | ✅ 完整 |
| 2 | 創建 Redis Cluster 實例 | 鍵值資料庫 Redis Cluster / 快速入門 | ✅ 完整 |
| 3 | 命令支持（指令支援） | 鍵值資料庫 Redis Cluster / 產品簡介 | ⚠️ 結構完整，逐格資料待補 |
| 4 | 參數支持（參數支援） | 鍵值資料庫 Redis Cluster / 產品簡介 | ⚠️ 結構完整，逐格資料待補 |

---

## 共用版面說明（四頁通用，designer 請先讀這段）

這四頁同屬「鍵值資料庫 Redis Cluster」文檔，共用同一套版面骨架：

1. **頂部導覽列（Header）**
   - 左：品牌 Logo ＋「文檔」入口（杜拜站＝Dubai Hybrid Cloud）。
   - 中：全站搜尋框，placeholder：「請輸入關鍵字快速獲取幫助」。
   - 右：備案、登入、以及主色調 CTA 按鈕「註冊有禮」。

2. **左側側邊欄（Sidebar，整個 Redis Cluster 文檔共用）**
   - 頂部標題：**鍵值資料庫 Redis Cluster**
   - 導覽樹（可折疊分組）：
     - **動態與公告** → 產品動態
     - **產品簡介** → 什麼是 Redis Cluster、產品優勢、功能特性、應用場景、產品架構、資源配置、產品版本、**參數支持**、**命令支持**
     - **計費指南** → 計費說明
     - **快速入門** → **創建 Redis Cluster 實例**、**連接 Redis Cluster 實例**
     - **操作指南** → 管理集群、配置集群、集群備份與恢復、升級集群、用戶管理 ACL、連接資料庫、執行命令、監控與告警、下載檔案
     - **最佳實踐**
     - **性能測試**
     - **常見問題**
   - 目前所在頁面在側邊欄以主色（綠色系）高亮。

3. **主內容區（置中欄）**
   - 頂部**麵包屑**：公有雲文檔中心 / 鍵值資料庫 Redis Cluster / <分組> / <頁面名>
   - **頁面標題**（H1）
   - 標題下方一行：**更新時間：YYYY-MM-DD HH:MM:SS**，同列右側有「PDF」下載圖示。
   - 內文（段落、章節標題、表格、提示框）。

4. **右側「本頁目錄」（On this page）**
   - 列出當頁各章節錨點，滑動時高亮目前章節。

5. **頁尾導覽**
   - 左下：**上一篇：<頁名>**；右下：**下一篇：<頁名>**。

6. **頁腳（Footer）**
   - 備案號等資訊列（杜拜站請替換為當地合規資訊）。

7. **提示框樣式（內文中會用到）**
   - ⚠️ **注意**（黃色底）：警示、不可逆或不可修改的操作。
   - ℹ️ **說明**（藍色底）：補充說明。

---

# 頁 1｜連接 Redis Cluster 實例

- **麵包屑**：公有雲文檔中心 / 鍵值資料庫 Redis Cluster / 快速入門 / 連接 Redis Cluster 實例
- **H1 標題**：連接 Redis Cluster 實例
- **更新時間**：2026-06-29 10:11:06（右側有 PDF 圖示）
- **本頁目錄（右欄）**：獲取連接位址、連接操作

**前言**

> Redis Cluster 支援多種連接方式，本文將為您介紹如何使用 redis-cli 連接 Redis 實例。

## 獲取連接位址

> Redis Cluster 實例建立完成後，進入 Redis Cluster 詳情頁面，選擇「節點」頁籤，即可看到 Redis 節點的 IP 位址，此 IP 位址即為連接位址，連接任一個主節點皆可。

**節點清單（表格區塊）**

- 表格上方操作列（按鈕）：`＋新增分片`、`＋新增副本`、`刪除`；右側搜尋框 placeholder：「請輸入節點名稱/ID」，並有重新整理圖示。
- 表格欄位：**節點名稱/ID｜區域｜角色｜雲伺服器類型｜節點狀態｜服務狀態｜配置｜IP｜安全組｜操作**
- 每列最左有勾選框；「操作」欄為「修改 | 刪除」連結。
- 狀態欄以綠點＋文字呈現（活躍／正常）。

**範例資料（供還原表格外觀用，非真實資料）**

| 節點名稱/ID | 區域 | 角色 | 雲伺服器類型 | 節點狀態 | 服務狀態 | 配置 | IP | 安全組 | 操作 |
|---|---|---|---|---|---|---|---|---|---|
| cln-98hx0hro | QA1A | 分片 | 性能型 | ● 活躍 | ● 正常 | 2核2G 110G | 172.20.253.24 | – | 修改 \| 刪除 |
| cln-aktutjfb | QA1A | cln-qjhwhwmp 副本 | 性能型 | ● 活躍 | ● 正常 | 2核2G 110G | 172.20.253.22 | – | 修改 \| 刪除 |
| cln-fs0kit9v | QA1A | 分片 | 性能型 | ● 活躍 | ● 正常 | 2核2G 110G | 172.20.253.26 | – | 修改 \| 刪除 |
| cln-neidnv23 | QA1A | cln-98hx0hro 副本 | 性能型 | ● 活躍 | ● 正常 | 2核2G 110G | 172.20.253.23 | – | 修改 \| 刪除 |
| cln-ogtvf9ek | QA1A | cln-fs0kit9v 副本 | 性能型 | ● 活躍 | ● 正常 | 2核2G 110G | 172.20.253.27 | – | 修改 \| 刪除 |
| cln-qjhwhwmp | QA1A | 分片 | 性能型 | ● 活躍 | ● 正常 | 2核2G 110G | 172.20.253.25 | – | 修改 \| 刪除 |

> 說明：角色欄有兩種值 —「分片」（主節點）與「<某節點ID> 副本」（該主節點的副本）。此範例為 3 分片 × 各 1 副本，共 6 節點。

## 連接操作

| 連接方式 | 說明 |
|---|---|
| 透過 redis-cli 連接 Redis 實例 | redis-cli 是原生 Redis 內建的命令列工具，您可以在與 Redis Cluster 實例位於同一個 VPC 網路下的雲伺服器上安裝 redis-cli，連接至 Redis Cluster 實例進行資料管理。 |

> 「透過 redis-cli 連接 Redis 實例」為連結文字（可點擊）。

**頁尾導覽**：上一篇：創建 Redis Cluster 實例 ｜ 下一篇：啟停集群

---

# 頁 2｜創建 Redis Cluster 實例

- **麵包屑**：公有雲文檔中心 / 鍵值資料庫 Redis Cluster / 快速入門 / 創建 Redis Cluster 實例
- **H1 標題**：創建 Redis Cluster 實例
- **更新時間**：2026-06-29 10:11:0X（右側有 PDF 圖示）
- **本頁目錄（右欄）**：前提條件、操作步驟、基礎配置、集群規格、購買資訊、網路設定、服務參數設定

**前言**

> 透過 AppCenter 集群管理控制台，您可以快速建立 Redis 集群。本小節主要介紹如何快速建立 Redis 集群。

## 前提條件

> 已註冊雲平台有效帳號並完成實名認證。

## 操作步驟

1. 透過 Web 瀏覽器登入青雲 QingCloud 管理控制台。
2. 在控制台頂部的導覽選單中，選擇「產品與服務 > 資料庫與快取 > 鍵值資料庫 Redis Cluster」。
   - 如果是首次部署，進入 Redis Cluster 應用介紹頁面，再進入應用管理器進入部署頁面。
   - 如果已建立過 Redis 集群，則進入 Redis Cluster 集群管理頁面，點擊「創建」進入部署頁面。
3. 在部署頁面，依頁面提示，設定應用基本屬性、應用資訊、網路資訊、環境參數等資訊：
   1. 基礎配置
   2. 集群規格
   3. 網路設定
   4. 服務參數設定
4. 確認配置與費用資訊無誤後，點擊「立即部署」，建立集群。
   集群建立完成後，於集群管理頁面查看與管理 Redis Cluster 集群。

**集群清單（範例，供還原表格外觀用）**

- 操作列按鈕：`＋創建`、`啟動`、`關閉`、`更多操作 ▾`；右側搜尋框 placeholder「請輸入集群名稱/ID」，並有標籤、重新整理、欄位設定等圖示。
- 表格欄位：**集群名稱/ID｜應用名稱/版本｜狀態｜區域｜節點數量｜網路｜計費方式｜標籤｜是否關聯告警｜操作**

| 集群名稱/ID | 應用名稱/版本 | 狀態 | 區域 | 節點數量 | 網路 | 計費方式 | 標籤 | 是否關聯告警 | 操作 |
|---|---|---|---|---|---|---|---|---|---|
| Redis Cluster / cl-cq9rcl6f | Redis Cluster / 7.0.4 – v2.0.0 | ● 活躍 | QA1A | 6 | AppCenter Metadata Service | 按小時計費 | cl-cq9rcl6f | – | 關閉 · 重啟 · ⋮ |

## 基礎配置

> 設定集群基本資訊，包括名稱、描述、選擇版本、計費方式與可用區。

| 參數 | 參數說明 |
|---|---|
| 區域 | 建議使用者選擇就近或本地區域，可減少網路延遲、提高存取速度。 |
| 版本 | 選擇集群版本；依版本不同，可選項目不同。 |
| 部署方式 | 選擇「多可用區部署」或「單可用區部署」，預設為單可用區部署。<br>・**多可用區部署**：將節點分散部署於當前區域的不同可用區，可用性高。<br>・**單可用區部署**：將節點部署於當前區域內的同一個可用區，網路延遲最低；可指定當前區域內的可用區。 |
| 可用區 | ・部署方式選擇「多可用區部署」時，需選擇多個可用區，指定部署可用區。<br>・部署方式選擇「單可用區部署」時，需選擇一個可用區，指定部署可用區。 |
| 名稱 | （可選）輸入自訂的集群名稱。預設為 `Redis Cluster`。 |
| 描述 | （可選）對集群的簡要描述。 |

## 集群規格

> 結合實際業務需求，設定節點資源類型或規格。不同 Redis 版本，節點配置參數略有不同：

- **Redis 4.0.6** 版本需選擇節點的記憶體、實例類型、硬碟類型、硬碟大小及主從節點數量。
- **Redis 5.0.8** 版本需選擇節點的 CPU、記憶體、實例類型、硬碟類型、硬碟大小及主從節點數量。
- **Redis 6.x** 版本及以上需選擇分片資源配置、記憶體、分片及分片副本數量。

> 關於如何設定節點的記憶體、類型、分片數量等，需由使用者結合實際業務需求進行選擇。

## 購買資訊

| 參數 | 參數說明 |
|---|---|
| 計費方式 | 選擇集群計費方式，可選擇按小時、包月、包年計費。 |
| 購買有效期 | 計費方式選擇「包年包月」時顯示此項，支援選擇：1 個月、3 個月、6 個月、1 年、2 年、3 年、4 年、5 年。 |
| 自動續約 | 計費方式選擇「包年包月」時顯示此項；若未勾選此項，設備到期後將改為按小時計費。 |

## 網路設定

> 透過網路設定獨享私有網路，便於網路存取控制，且不影響其它私有網路的設定，可確保資料庫於不同業務間進行網路隔離。資料庫集群僅可加入已連接的私有網路，且需確保該私有網路的 DHCP 處於開啟狀態。

| 參數 | 參數說明 |
|---|---|
| VPC 網路 | 設定 VPC 網路。<br>・預設載入區域內已有的 VPC 網路，可於下拉框選擇已有 VPC 網路。<br>・若無可選網路，可點擊「創建」，建立該網路。<br>⚠️ **注意**：請勿選擇免費型的 VPC，目前不支援在免費型 VPC 中建立集群。 |
| 私有網路 | 選擇私有網路。<br>・預設載入區域內已有的私有網路，可於下拉框選擇已有私有網路。<br>・若無可選網路，可點擊「創建」，建立該網路。<br>ℹ️ **說明**：私有網路的部署方式應與集群的部署方式保持一致，即皆為多可用區部署或皆為單可用區部署。 |
| 安全組 | （可選）點擊選擇，於彈出的「選擇安全組」視窗選定安全組即可。支援多選。 |
| 節點 IP | 設定節點 IP 位址。<br>・預設為自動分配。<br>・選擇「手動配置」為各節點配置 IP。 |
| 預留 IP | 設定集群預留高可用 IP 位址。<br>・預設為自動分配。<br>・選擇「手動配置」為集群配置高可用 IP。 |

## 服務參數設定

| 參數 | 參數說明 |
|---|---|
| 禁用 FLUSH 命令 | 選擇是否禁用 `FLUSHALL` 及 `FLUSHDB` 命令。預設為「否」，表示不禁用。<br>⚠️ **注意**：<br>・此參數在集群建立後不可修改。<br>・由於該操作命令的呼叫會對資料造成不可恢復的影響，因此建議在生產環境下禁用此命令。<br>・Redis 5.0.10 版本開始支援此參數。 |
| 控制建立管理 ACL | 選擇是否啟用「控制管理 ACL」服務。Redis 6.2.5 版本開始支援此參數。<br>・預設為「是」，開啟控制管理 ACL 服務，允許透過命令建立 ACL 帳號。<br>・「否」表示關閉控制管理 ACL 服務，支援透過命令建立 ACL 帳號。 |
| 更多服務環境參數 | 點擊展開更多參數列，可設定更多服務參數。可設定參數與資料庫效能相關，部分參數修改會導致資料庫服務重啟，詳情請見參數說明。 |

**頁尾導覽**：上一篇：計費說明 ｜ 下一篇：連接 Redis Cluster 實例

---

# 頁 3｜命令支持（指令支援）

- **麵包屑**：公有雲文檔中心 / 鍵值資料庫 Redis Cluster / 產品簡介 / 命令支持
- **H1 標題**：命令支持
- **更新時間**：標題下一行（右側有 PDF 圖示）
- **本頁目錄（右欄）**：依指令分類（Bitmap、Cluster、Connection、Geo、Hash…）各為一個錨點。

> ⚠️ **給 designer 的說明**：本頁為「指令 × 版本」的大型對照表，這張是**全頁截圖、字級極小**。表格「版面結構」已可確定，但**每一格的 ✓/✗ 支援狀態與每一條指令名稱無法從縮圖逐格可靠判讀**。下方逐格值請以**原始資料或高清截圖**填入，切勿臆測。

**前言（大意，需以原文校對）**

> Redis Cluster 支援豐富的原生 Redis 指令。不同 Redis 版本所支援的指令略有不同，本頁以對照表列出各指令在各版本的支援情況。

**表格結構（可確定）**

- 全頁由多個**指令分類區塊**組成，每個分類是一個小標題＋一張表。
- 每張表欄位：**命令（指令名）｜說明｜Redis 4.0.6｜Redis 5.0.8｜Redis 6.x｜Redis 7.x**
- 支援狀態欄以 `✓`（支援）／`✗`（不支援）呈現。
- 指令分類（依 Redis 官方指令群，實際分類與順序需照原頁核對）：
  - Bitmap 命令
  - Cluster 命令
  - Connection 命令
  - Geo 命令
  - Hash 命令
  - HyperLogLog 命令
  - Keys（鍵）命令
  - Lists 命令
  - Pub/Sub 命令
  - Scripting 命令
  - Server 命令
  - Set 命令
  - Sorted Set 命令
  - Streams 命令
  - Strings 命令
  - Transactions 命令

**逐格內容**：⚠️ 待補（需高清原始資料）。範例列格式如下，供 designer 先排版：

| 命令 | 說明 | Redis 4.0.6 | Redis 5.0.8 | Redis 6.x | Redis 7.x |
|---|---|:---:|:---:|:---:|:---:|
| `<指令名>` | `<指令說明>` | ✓/✗ | ✓/✗ | ✓/✗ | ✓/✗ |

---

# 頁 4｜參數支持（參數支援）

- **麵包屑**：公有雲文檔中心 / 鍵值資料庫 Redis Cluster / 產品簡介 / 參數支持
- **H1 標題**：參數支持
- **更新時間**：標題下一行（右側有 PDF 圖示）
- **本頁目錄（右欄）**：對應頁面各分段錨點。

> ⚠️ **給 designer 的說明**：本頁與頁 3 **是不同頁面** — 頁 3 是「**指令**（command）支援對照」、頁 4 是「**參數**（config parameter）支援對照」。本頁同為全頁截圖、字級極小，**版面結構可確定，但每一格 ✓/✗ 與部分參數名稱無法逐格可靠判讀**，逐格值請以原始資料填入。

**前言（大意，需以原文校對）**

> Redis Cluster 支援多種參數配置能力，不同版本支援的參數略有不同。本頁以對照表列出各參數在各版本的支援情況與說明。

**表格結構（可確定）**

- 單一大型對照表（可能含少量分段）。
- 欄位：**參數（參數名）｜說明｜Redis 4.0.6｜Redis 5.0.8｜Redis 6.x｜Redis 7.x**
- 支援狀態欄以 `✓`（支援）／`✗`（不支援）呈現。
- 部分列的「說明」欄內含 ℹ️ 說明框或 ⚠️ 注意框（藍色／黃色底），並列出該參數的可選值（如 `yes/no`、預設值等）。

**可辨識到的參數名（部分，需以原頁校對；保留原文）**

> 以下為從截圖可大致辨識的參數，僅供對位參考，**支援狀態一律待補**：
> `maxmemory`、`maxmemory-policy`、`maxmemory-samples`、`maxclients`、`timeout`、`tcp-backlog`、`tcp-keepalive`、`databases`、`hash-max-ziplist-entries`、`hash-max-ziplist-value`、`list-max-ziplist-size`、`set-max-intset-entries`、`zset-max-ziplist-entries`、`zset-max-ziplist-value`、`activerehashing`、`appendonly`、`appendfsync`、`notify-keyspace-events`、`slowlog-log-slower-than`、`lua-time-limit` …等。

**逐格內容**：⚠️ 待補（需高清原始資料）。範例列格式如下，供 designer 先排版：

| 參數 | 說明 | Redis 4.0.6 | Redis 5.0.8 | Redis 6.x | Redis 7.x |
|---|---|:---:|:---:|:---:|:---:|
| `<參數名>` | `<參數說明／可選值／預設值>` | ✓/✗ | ✓/✗ | ✓/✗ | ✓/✗ |

---

## 頁 3 與頁 4 的差異（給 designer 一句話）

- **頁 3｜命令支持**：對照的是 **Redis 指令（commands）**，依 Bitmap / Cluster / Hash / Set … 等**指令分類**分成多張表。
- **頁 4｜參數支持**：對照的是 **Redis 設定參數（config parameters）**，為**單一大表**，「說明」欄常含可選值與提示框。
- 兩頁欄位骨架相同（名稱｜說明｜4.0.6｜5.0.8｜6.x｜7.x），但**內容主體不同**（一個是指令、一個是參數）。
